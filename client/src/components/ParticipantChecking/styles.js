import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    wrapper: {
        margin: '20px',
        padding: '20px'
    },
    dialogContent: {
        margin: '20px'
    }
}));

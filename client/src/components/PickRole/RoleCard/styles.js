import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    paper: {
        padding: '20px',
        textAlign: 'center',
    },
    media: { width: '100px', height: '100px', marginBottom: '20px' },
}));
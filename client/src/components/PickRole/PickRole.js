import React from 'react';
//import makeStyles in the last
import makeStyles from './styles';

const PickRole = () => {
    const css = makeStyles();
    return <div>PickRole</div>;
};

export default PickRole;

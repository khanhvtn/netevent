import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    textField: {
        marginBottom: '20px'
    },
    dialogActions: {
        marginRight: '20px'
    }
}));

import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    wrapper: {
        flexGrow: 1
    },
    circularProgress: {
        position: 'absolute',
        top: '50%',
        left: '50%'
    }
}));
